// Calculate net sales where net sales = gross sales � 10% discount of gross sales.
 #include <stdio.h>
 int main()  {
 	int gross;
 	printf("gross sales :");
 	scanf("%d",&gross);
	printf("net sales = %f",0.9*gross);
}